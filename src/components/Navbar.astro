---
const navLinks = [
  { label: 'Wellness', href: '/blog?category=wellness' },
  { label: 'Longevity', href: '/blog?category=longevity' },
  { label: 'Nutrition', href: '/blog?category=nutrition' },
  { label: 'Mind & Body', href: '/blog?category=mind-body' },
];

const currentPath = Astro.url.pathname;
---

<header class="sticky top-0 z-50 bg-cream/95 backdrop-blur-sm border-b border-border">
  <nav class="max-w-6xl mx-auto px-6 py-4 flex items-center justify-between">
    <!-- Logo -->
    <a href="/" class="flex items-center gap-2.5 group">
      <img src="/logo.svg" alt="" width="38" height="38" class="flex-shrink-0 transition-transform group-hover:scale-105 duration-200" />
      <span class="text-2xl font-heading font-semibold text-bark group-hover:text-sage-dark transition-colors">
        Best<span class="text-sage">Karma</span>
      </span>
    </a>

    <!-- Desktop nav -->
    <ul class="hidden md:flex items-center gap-8">
      {navLinks.map(link => (
        <li>
          <a
            href={link.href}
            class="text-stone hover:text-sage-dark font-medium text-sm tracking-wide transition-colors"
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>

    <!-- CTA -->
    <div class="flex items-center gap-4">
      <a
        href="/blog"
        class="hidden md:inline-block text-sm font-medium text-stone hover:text-bark transition-colors"
      >
        Articles
      </a>
      <a
        href="/#newsletter"
        class="inline-flex items-center gap-2 bg-sage text-cream text-sm font-medium px-5 py-2.5 rounded-full hover:bg-sage-dark transition-colors"
      >
        <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect width="20" height="16" x="2" y="4" rx="2"/>
          <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
        </svg>
        Newsletter
      </a>

      <!-- Mobile menu button -->
      <button
        id="mobile-menu-btn"
        class="md:hidden p-2 rounded-lg hover:bg-cream-dark transition-colors"
        aria-label="Toggle menu"
        aria-expanded="false"
      >
        <svg id="icon-open" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="4" x2="20" y1="12" y2="12"/>
          <line x1="4" x2="20" y1="6" y2="6"/>
          <line x1="4" x2="20" y1="18" y2="18"/>
        </svg>
        <svg id="icon-close" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="hidden">
          <path d="M18 6 6 18"/>
          <path d="m6 6 12 12"/>
        </svg>
      </button>
    </div>
  </nav>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="hidden md:hidden border-t border-border bg-cream">
    <ul class="max-w-6xl mx-auto px-6 py-4 flex flex-col gap-4">
      {navLinks.map(link => (
        <li>
          <a
            href={link.href}
            class="block text-stone hover:text-sage-dark font-medium py-2 text-base transition-colors"
          >
            {link.label}
          </a>
        </li>
      ))}
      <li class="pt-2 border-t border-border">
        <a href="/blog" class="block text-stone hover:text-bark font-medium py-2 text-base transition-colors">
          All Articles
        </a>
      </li>
    </ul>
  </div>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  const iconOpen = document.getElementById('icon-open');
  const iconClose = document.getElementById('icon-close');

  btn?.addEventListener('click', () => {
    const isOpen = menu?.classList.contains('hidden');
    menu?.classList.toggle('hidden', !isOpen);
    iconOpen?.classList.toggle('hidden', isOpen);
    iconClose?.classList.toggle('hidden', !isOpen);
    btn.setAttribute('aria-expanded', String(isOpen));
  });
</script>
